spring:
  application:
    name: pricing-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/mortgage_pricing?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

app:
  messaging:
    enabled: ${APP_MESSAGING_ENABLED:true}
    consumer-enabled: ${APP_MESSAGING_CONSUMER_ENABLED:true}
    queue-name: ${APP_MESSAGING_QUEUE_NAME:quote-pricing-requests}
    endpoint: ${AWS_ENDPOINT:http://localhost:4566}
    region: ${AWS_REGION:us-east-1}
    poll-delay-ms: ${APP_MESSAGING_POLL_DELAY_MS:3000}
  lead-messaging:
    enabled: ${APP_LEAD_MESSAGING_ENABLED:true}
    queue-name: ${APP_LEAD_MESSAGING_QUEUE_NAME:quote-lead-requests}
    endpoint: ${AWS_ENDPOINT:http://localhost:4566}
    region: ${AWS_REGION:us-east-1}
  pricing-result-messaging:
    enabled: ${APP_PRICING_RESULT_MESSAGING_ENABLED:true}
    queue-name: ${APP_PRICING_RESULT_QUEUE_NAME:quote-pricing-results}
    endpoint: ${AWS_ENDPOINT:http://localhost:4566}
    region: ${AWS_REGION:us-east-1}
  service-token:
    secret: ${APP_SERVICE_TOKEN_SECRET:dev-only-service-token-secret-change-me-1234567890}
    issuer: ${APP_SERVICE_TOKEN_ISSUER:mortgage-loan-api}
    audience: ${APP_SERVICE_TOKEN_AUDIENCE:pricing-service}
    scope: ${APP_SERVICE_TOKEN_SCOPE:pricing:write}
  outbound-service-token:
    secret: ${APP_OUTBOUND_SERVICE_TOKEN_SECRET:dev-only-lead-service-token-secret-change-me-1234567890}
    issuer: ${APP_OUTBOUND_SERVICE_TOKEN_ISSUER:pricing-service}
    audience: ${APP_OUTBOUND_SERVICE_TOKEN_AUDIENCE:lead-service}
    scope: ${APP_OUTBOUND_SERVICE_TOKEN_SCOPE:lead:write}
    ttl-seconds: ${APP_OUTBOUND_SERVICE_TOKEN_TTL_SECONDS:300}
  pricing-result-token:
    secret: ${APP_PRICING_RESULT_TOKEN_SECRET:dev-only-pricing-result-secret-change-me-1234567890}
    issuer: ${APP_PRICING_RESULT_TOKEN_ISSUER:pricing-service}
    audience: ${APP_PRICING_RESULT_TOKEN_AUDIENCE:mortgage-loan-api}
    scope: ${APP_PRICING_RESULT_TOKEN_SCOPE:quote:write}
    ttl-seconds: ${APP_PRICING_RESULT_TOKEN_TTL_SECONDS:300}

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8084
